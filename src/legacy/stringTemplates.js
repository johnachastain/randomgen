const a = `The ${1} and the ${2} ${3}`
const b = `The ${1} and ${2} ${3}`
const c = `The ${1} ${2}`
const d = `The ${1} ${2} ${3}`

const e = `of the ${1}`
const f = `of the ${1} ${2}`
const g = `of ${1}`
const h = `of ${1} ${2}`

const i = ` the ${1}`